import Container from "../ui/Container";

function QueryResultViewer() {
  return (
    <Container $gridArea="query-result-viewer">Query Result Viewer</Container>
  );
}

export default QueryResultViewer;
